apiVersion: apps/v1
kind: Deployment
metadata:
  name: sample-app
spec:
  progressDeadlineSeconds: 9900      
  replicas: 3
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: sample-app
  template:
    metadata:
      labels:
        app: sample-app
    spec:
      containers:
      - image: gitopsbook/sample-app:v0.1
        name: sample-app
        ports:
        - containerPort: 8080
